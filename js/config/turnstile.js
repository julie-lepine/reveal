/** Site Key Cloudflare Turnstile (publique). Secret Key = Supabase → Attack Protection uniquement. */
export const TURNSTILE_SITE_KEY = "0x4AAAAAADX_7XllBAkmx7Xg";

/** true = désactive Turnstile partout (urgence). false = actif sauf localhost / ?skipCaptcha=1. */
export const TURNSTILE_DISABLED = false;
